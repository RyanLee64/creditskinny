import React from 'react';

const About = (user) => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Credit Skinny was borne out of a marketing project in 2019. The goal was simple. Equip college students with the tools they need to maximize credit rewards, stay out of debt for ever, and have a better future as a result.</h1>
    </div>
  );
};

export default About;
